github:
  ScriptonBasestar:
    # default false
    private: false
    targetPath: $HOME/mywork/ScriptonBasestar
    # default include
    default:
      strategy: include
      branch: develop
    include:
      proxynd:
        branch: develop
      devops-minim-engine:
        branch: dev
    exclude:
      # regex
      - sb-wp-*
    override:
      include:
  nginxinc:
    targetPath: $HOME/mywork/nginxinc
